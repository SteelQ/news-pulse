# Node.js 20 LTS 镜像，用于 Vue 3 + Vite 开发
FROM node:20-alpine

# 设置工作目录
WORKDIR /app

# 安装全局工具（可选）
# RUN npm install -g @vitejs/plugin-vue

# 暴露 Vite 开发服务器端口
EXPOSE 5173

# 默认命令（在 docker-compose 中会被覆盖）
CMD ["npm", "run", "dev", "--", "--host"]
